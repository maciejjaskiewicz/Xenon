#pragma once

#include <string>

namespace Xenon
{
    struct RendererAPIDetails
    {
        std::string name;
        std::string version;
        std::string vendor;
        std::string renderer;
    };
}
